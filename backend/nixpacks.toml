# Nixpacks configuration for Railway deployment
# ffmpeg is required for audio compression (Whisper 25MB limit)

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
